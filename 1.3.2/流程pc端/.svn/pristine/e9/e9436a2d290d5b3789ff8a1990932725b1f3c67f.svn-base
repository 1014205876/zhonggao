import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ReadOnlyBlockComponent } from './read-only-block.component';

describe('ReadOnlyBlockComponent', () => {
  let component: ReadOnlyBlockComponent;
  let fixture: ComponentFixture<ReadOnlyBlockComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ReadOnlyBlockComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ReadOnlyBlockComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
